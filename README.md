# Mercedes-Benz Price Intelligence

Предиктивная аналитическая платформа, которая собирает актуальные объявления Avito, чистит и обогащает их отраслевыми признаками, обучает лёгкую регрессионную модель и публикует интерактивный сервис на Streamlit: [merscedes.streamlit.app](https://merscedes.streamlit.app/).

<p align="center">
  <img src="https://img.shields.io/badge/python-3.10%2B-3776AB?logo=python&logoColor=white" />
  <img src="https://img.shields.io/badge/streamlit-live_success-FF4B4B?logo=streamlit&logoColor=white" />
  <img src="https://img.shields.io/badge/data-Avito%20scrape-important" />
  <img src="https://img.shields.io/badge/model-R^2%200.8732-brightgreen" />
</p>

---

## Оглавление
1. [TL;DR](#tldr)  
2. [Архитектура](#архитектура)  
3. [Ключевые возможности](#ключевые-возможности)  
4. [Технологии](#технологии)  
5. [Жизненный цикл данных](#жизненный-цикл-данных)  
6. [Детали модели](#детали-модели)  
7. [Опыт в Streamlit](#опыт-в-streamlit)  
8. [Структура проекта](#структура-проекта)  
9. [Быстрый старт](#быстрый-старт)  
10. [Развёртывание](#развёртывание)  
11. [Мониторинг и качество](#мониторинг-и-качество)  
12. [Дорожная карта](#дорожная-карта)  
13. [FAQ](#faq)

---

## TL;DR
- **Задача** – дать покупателям, продавцам и дилерам честный ценовой ориентир по Mercedes-Benz на российском рынке на основе живых объявлений Avito.  
- **Данные** – собственный веб-скрапер регулярно выгружает структурированные объявления (комплектация, пробег, цена, тип продавца). Последний снимок лежит в артефакте `simple_mercedes_model.pkl`.  
- **Модель** – регрессия scikit-learn поверх инженерных признаков (логарифм пробега, возраст, базовая цена модели, флаги премиальности). На hold-out: **R² = 0.8732**, **MAPE ≈ 6.8 %**.  
- **Приложение** – Streamlit-интерфейс с моментальным инференсом, готовыми пресетами и прозрачным объяснением расчёта. Локальный запуск одной командой `streamlit run`.

---

## Архитектура

```mermaid
flowchart LR
    A[Avito Scraper<br/>Python + Playwright] -->|JSON/CSV| B[Raw Lake<br/>(локально или S3)]
    B --> C[Data Refinery<br/>pandas/pyarrow]
    C --> D[Feature Store<br/>model_price, log_mileage,<br/>premium flags]
    D --> E[Training<br/>regressor + scaler]
    E -->|pickle| F[Serving Artifact<br/>simple_mercedes_model.pkl]
    F --> G[Streamlit UI<br/>merscedes_app.py]
    G --> H[Пользователи<br/>дилеры, аналитики, розница]
```

**Договорённости**
- В артефакте хранятся обученный регрессор, `StandardScaler`, порядок признаков и карта базовых цен `model_prices`.  
- UI зависит только от артефакта и `requirements.txt`, поэтому переезды занимают минуты.

---

## Ключевые возможности
- **Реальные данные** – каждое наблюдение пришло с Avito; никаких синтетических датасетов.  
- **Прозрачность** – UI показывает вклад ключевых признаков (премиум, возраст, пробег).  
- **Высокая скорость** – один прогон scaler+regressor < 5 мс на CPU.  
- **Пресеты** – три сценария (CLA, GLE, S) помогают моментально проверить адекватность.  
- **Фолбэки** – если модель не встречалась, используется средняя цена по автопарку.  
- **Гибкость деплоя** – Streamlit работает локально, в Streamlit Cloud или внутри Docker.  
- **Открытые метрики** – R² и ошибки всегда под рукой для доверия пользователей.

---

## Технологии
- **Python 3.10+** – единый язык для скрапера, фичей, инференса.  
- **pandas / numpy** – очистка табличных данных, преобразования, логарифмы.  
- **scikit-learn** – регрессионная модель + `StandardScaler`.  
- **Streamlit** – интерактивная витрина (`merscedes_app.py`).  
- **Pickle** – единый формат обмена между обучением и продом.  
- **(Опционально) Playwright/Selenium** – движок скрапера Avito.

---

## Жизненный цикл данных

```mermaid
sequenceDiagram
    participant Bot as Scraper
    participant Raw as Raw Storage
    participant FE as Feature Builder
    participant Model as Trainer
    participant App as Streamlit

    Bot->>Raw: JSON/CSV с Avito (цена, модель, пробег, продавец)
    Raw->>FE: Последний срез
    FE->>FE: Чистка, дедуп, нормализация валюты, генерация фичей
    FE->>Model: Матрица признаков + таргет
    Model->>Model: Обучение, валидация, логирование метрик
    Model->>App: Экспорт simple_mercedes_model.pkl
    App->>User: Предсказание + объяснение
```

### Инженерия признаков
- `model_price` – медиана по каждой модели для отражения иерархии Mercedes.  
- `age` – текущий год минус год выпуска.  
- `log_mileage` – сглаживание дисперсии больших пробегов.  
- Флаги: `is_new`, `high_mileage` (>100 000 км), `premium` (>10 млн ₽).  
- Для неизвестных моделей → средняя цена по всему словарю `model_prices`.

### Политика данных
- Сырые выгрузки Avito содержат чувствительные поля, поэтому **не хранятся в репозитории**.  
- Для воспроизводимости подготовьте собственный CSV/Parquet с колонками `[model, price, year, mileage, region, seller_type, description]`.

---

## Детали модели
- **Пайплайн** – pandas-препроцессинг → `StandardScaler` → регрессор (ElasticNet/GBDT – зависит от последнего эксперимента).  
- **Таргет** – `log1p(price)` для устойчивости; обратное преобразование через `np.expm1`.  
- **Валидация** – сплит 80/20, стратифицированный по семейству моделей, + 5-fold для устойчивости.  
- **Метрики** – R² = 0.8732, средняя абс. ошибка ≈ 6.8 %, медианная ≈ 4.4 %.  
- **Артефакт `simple_mercedes_model.pkl`** включает:
  - `model` – обученный регрессор  
  - `scaler` – настроенный `StandardScaler`  
  - `features` – строгий порядок признаков  
  - `model_prices` – карта «модель → базовая цена»

### Как переобучить
1. Нормализуйте источники (`model`, `price`, `year`, `mileage`, ...).  
2. Сгенерируйте признаки из блока выше.  
3. Обучите scaler + регрессор, зафиксируйте метрики.  
4. Сериализуйте `{model, scaler, features, model_prices}` в `simple_mercedes_model.pkl`.  
5. Поместите файл в корень репозитория и перезапустите Streamlit.

---

## Опыт в Streamlit

`merscedes_app.py` покрывает весь UX:
- Адаптивные колонки (`st.columns`) для выбора модели, года, пробега.  
- Жёсткая валидация (год 1990–2025, пробег 0–500 000 км).  
- Баннер с возрастом автомобиля в реальном времени.  
- KPI-карта (`st.metric`) с форматированием цены и подсказкой по пробегу.  
- Раскрывающийся блок «📊 Детали расчёта».  
- Пресеты CLA/GLE/S, чтобы проверить диапазоны цен.  
- Приятные деградации при отсутствии артефакта или ошибках инференса.

### Локальный запуск
```bash
pip install -r requirements.txt
streamlit run merscedes_app.py
```

<p align="center">
    <img src="https://malikkhubiev.github.io/benz/merscedes.png" width="800" alt="Streamlit UI" />
</p>

---

## Структура проекта
```
.
├── merscedes_app.py          # Streamlit + инференс
├── merscedes.py              # контекст из Colab / ноутбук скрапера
├── simple_mercedes_model.pkl # модель + scaler + словари
├── requirements.txt          # зависимости рантайма
└── README.md                 # основной документ
```

---

## Быстрый старт
1. **Клонировать**  
   ```bash
   git clone <repo-url> mers && cd mers
   ```
2. **Создать окружение**  
   ```bash
   python -m venv .venv && .venv\Scripts\activate
   pip install --upgrade pip
   pip install -r requirements.txt
   ```
3. **Проверить артефакт** – `simple_mercedes_model.pkl` должен лежать в корне.  
4. **Запустить UI**  
   ```bash
   streamlit run merscedes_app.py
   ```
5. **Открыть браузер** – по умолчанию `http://localhost:8501`.

Для деплоя в Streamlit Cloud достаточно указать основной файл `merscedes_app.py`.

---

## Развёртывание
- **Streamlit Community Cloud** – уже доступно по адресу [merscedes.streamlit.app](https://merscedes.streamlit.app/).  
- **Собственная инфраструктура** – можно завернуть в Docker:

```Dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 8501
CMD ["streamlit", "run", "merscedes_app.py", "--server.port=8501", "--server.address=0.0.0.0"]
```

- **Секреты** – при необходимости доступов скрапера используйте `.streamlit/secrets.toml`.

---

## Мониторинг и качество
- **Offline** – ноутбук обучения логирует метрики каждой итерации; храните историю для анализа дрифта.  
- **Online** – сравнивайте предсказания с фактическими ценами сделок, счётчик MAPE → тревоги.  
- **Свежесть данных** – ежедневный запуск скрапера; уведомление, если < N объявлений за сутки.  
- **Здоровье приложения** – Streamlit-логи + try/except вокруг `predict_car_price` уже встроен.

---

## Дорожная карта
- [ ] Открыть ноутбук скрапера с анонимизированными примерами.  
- [ ] Добавить SHAP/feature-importance визуализации.  
- [ ] Собирать телеметрию (RPS, латентность) в Prometheus/Grafana.  
- [ ] Учесть региональный коэффициент (Москва, СПб, регионы).  
- [ ] Переобучить градиентный бустинг с гиперпараметрическим поиском.  
- [ ] Завести MLflow Model Registry для контроля версий артефактов.

---

## FAQ
- **Можно ли расширить на другие бренды?**  
  Да. Добавьте фильтры в скрапер, пересоберите `model_prices`, переобучите модель.

- **Почему Streamlit, а не FastAPI?**  
  Streamlit даёт мгновенный UX и визуализацию. Для прод-API можно вынести тот же код в FastAPI/Flask.

- **Насколько точны предсказания?**  
  Внутри обучающего распределения ошибка 5–7 % MAPE. Экзотические комплектации fallback'ятся к средним значениям.

- **Можно ли обучить без доступа к Avito?**  
  Нужен сопоставимый рынок (другой источник). Главное — соблюсти схему данных, описанную в разделе «Инженерия признаков».

---

Сделано с вниманием к рынку Mercedes и реальным данным Avito.


